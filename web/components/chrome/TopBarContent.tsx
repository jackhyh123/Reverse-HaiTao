"use client";

/**
 * Top bar — 现在只是一条占位的 chrome bar；
 * 语言/主题/高级设置已迁移到左下角侧栏的「设置」入口。
 */
export default function TopBarContent({
  mode: _mode,
}: {
  mode: "workspace" | "utility";
}) {
  return null;
}
